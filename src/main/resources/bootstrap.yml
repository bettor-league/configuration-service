server:
  port: 1000

spring:
  profiles:
    active: develop
  cloud:
    config:
      server:
        git:
          uri: https://github.com/bettor-league/configuration-repository
          default-label: develop
